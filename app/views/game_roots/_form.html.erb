<%= form_with(model: game_root, local: true) do |form| %>
  <% if game_root.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_root.errors.count, "error") %> prohibited this game_root from being saved:</h2>

      <ul>
        <% game_root.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :available %>
    <%= form.text_field :available %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
