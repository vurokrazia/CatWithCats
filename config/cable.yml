redis: &redis
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  
development: *redis
  #channel_prefix: catitos_production

test:
  adapter: test

production: *redis
  #channel_prefix: catitos_production